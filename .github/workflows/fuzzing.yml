name: Fuzzing

on:
  push:

jobs:
  fuzz:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get install -y ninja-build cmake clang libssl-dev libcurl4-openssl-dev nlohmann-json3-dev

      - name: Configure with libFuzzer
        env:
          CC: clang
          CXX: clang++
        run: |
          cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_FLAGS="-fsanitize=fuzzer-no-link,address" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address"

      - name: Build
        run: cmake --build build

      - name: Run fuzz targets
        run: echo "Placeholder — fuzz targets added when parsing/crypto input logic exists"
